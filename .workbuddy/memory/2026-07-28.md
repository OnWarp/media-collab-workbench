# 2026-07-28

## 优化：添加 GitHub Actions 自动部署 + 清理

- 新增 `.github/workflows/deploy-worker.yml`：推送 `main` 即 `wrangler deploy --config wrangler.jsonc`。
  - 含 ESM 语法校验（`cp worker.js worker.check.mjs && node --check`，因仓库无 `type:module` 且 server.js 是 CJS）、幂等创建 R2 桶、可选注入 `BOOTSTRAP_TOKEN` 密钥、concurrency 并发保护。
  - 需要仓库 Secrets：`CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`；`BOOTSTRAP_TOKEN` 可选（用于 `/api/bootstrap` 建首个管理员）。
- 新增 `.gitignore`，并 `git rm --cached` 停止追踪含真实管理员密码哈希的 `data/db.json` 与冗余嵌套目录 `media-collab-workbench/`（二者仍保留在本地磁盘）。
- `package.json` 增加 `wrangler@^4` 为 devDependency，使 CI/`npm install` 可获取部署工具。
- 已本地提交 `61c9544`。推送 `origin/main` 在本沙箱被拦截（无 GitHub 凭据、交互式 prompt 无法运行），需用户在自有终端执行 `git push origin main` 激活流水线，并在仓库 Settings 配置上述 3 个 Secrets。
- 安全提醒：`data/db.json` 仍留在 git 历史中；若仓库曾公开，应轮换管理员密码并考虑清理历史。
