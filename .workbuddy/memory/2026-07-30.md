# 2026-07-30 工作日志

## CI 部署打通 + 管理员建号排查

- **部署已成功**：用户填齐 `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`/`BOOTSTRAP_TOKEN`/`D1_DATABASE_ID`/`ADMIN_USERNAME`/`ADMIN_PASSWORD` 后重跑 CI，`wrangler deploy` 成功上线 `https://media-collab-workbench.owarp.workers.dev`。`deleted_classes` 删除 AppState DO 的迁移生效，不再报 code 10064。
- **管理员没建出来的根因**：`ADMIN_PASSWORD` 不足 10 位。CI 自动建管理员步骤（deploy-worker.yml 第 115 行）`if [ ${#ADMIN_PASSWORD} -lt 10 ]` 静默跳过，且不报错，导致用户误以为流程正常但未建号。修复：把 `ADMIN_PASSWORD` 改为 ≥10 位后重跑（或手动 `POST /api/bootstrap`）。
- **Worker 密码规则（来自 worker.mjs）**：密码至少 10 位；用户名 3–32 位且仅 `[A-Za-z0-9_-]`；PBKDF2(SHA-256, 21万次)+随机 salt 哈希存储。`/api/bootstrap` 仅在 users 表为空时可用一次，需请求头 `x-bootstrap-token` 等于 `BOOTSTRAP_TOKEN` secret。
- **易错点提醒**：GitHub Secrets 改动不会自动触发 CI，需 push 或 Actions→Run workflow 才会重跑；`wrangler secret put` 只在部署步骤执行，所以改 Secret 后必须重跑部署才会注入 Worker。
