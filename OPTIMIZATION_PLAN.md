# 项目优化计划

> 更新时间：2026-07-31

---

## 一、已完成的修复 ✅

### 1.1 高危Bug修复

| 问题 | 修复内容 | 文件位置 |
|------|----------|----------|
| **永久删除后Modal不关闭** | `act()` 函数添加返回值，永久删除按钮使用async/await正确处理 | `TopicDetail.tsx:66-76, 99-104` |
| **媒体链接类型选择器失效** | 添加 `mediaType` 状态管理，修复Select组件双向绑定 | `Modals.tsx:172-203` |
| **Modal组件API失败永久加载** | 为所有Modal添加 `.catch()` 错误处理和 `cancelled` 标志防止内存泄漏 | `Modals.tsx` 多处 |
| **数据写入无原子性** | 实现先写 `.tmp` 文件再 `rename` 的原子写入机制 | `server.js:29-39` |

### 1.2 安全修复

| 问题 | 修复内容 | 文件位置 |
|------|----------|----------|
| **登录暴力破解** | 添加IP级速率限制（15分钟窗口，最多5次尝试） | `server.js:74-92` |
| **CSV注入漏洞** | 添加 `escapeCSV()` 函数，对 `=+-@` 开头的字段转义 | `server.js:110-117` |
| **密码强度不足** | 注册和管理员创建用户时强制密码≥10位，用户名3-32位 | `server.js:355-370, 451-464` |

### 1.3 性能优化

| 问题 | 优化内容 | 文件位置 |
|------|----------|----------|
| **nextId()栈溢出风险** | 使用计数器替代 `Math.max(...spread)` | `server.js:34-38` |

### 1.4 代码质量

| 问题 | 优化内容 | 文件位置 |
|------|----------|----------|
| **重复代码** | `fmtTime/fmtMoney` 统一到 `api.ts`，`common.tsx` 重新导出 | `common.tsx:1-5` |

---

## 二、已添加的测试框架 🧪

### 2.1 Vitest配置

- 添加了 Vitest 测试依赖到 `package.json`
- 创建 `vitest.config.ts` 配置文件
- 创建 `src/test/setup.ts` 测试设置文件

### 2.2 测试用例

- 创建 `src/api.test.ts` 包含工具函数测试
- 测试覆盖：`looseUrl`、`fmtTime`、`fmtMoney`、`stageOrder`

---

## 三、待优化项目 🔄

### 3.1 高优先级

| 优化项 | 说明 | 预计工作量 |
|--------|------|------------|
| **Board页面API优化** | 合并3个独立API请求为1-2个，减少网络开销 | 2小时 |
| **添加更多测试用例** | 覆盖核心业务逻辑和组件 | 4小时 |
| **TypeScript类型优化** | 减少any类型使用，增强类型安全 | 2小时 |

### 3.2 中优先级

| 优化项 | 说明 | 预计工作量 |
|--------|------|------------|
| **Context性能优化** | 使用useMemo缓存value，避免不必要重渲染 | 2小时 |
| **组件拆分** | Modals.tsx (741行) 拆分为多个独立组件 | 4小时 |
| **错误边界** | 添加React Error Boundary处理组件错误 | 1小时 |
| **日志清理机制** | server.js添加logs自动清理功能 | 1小时 |

### 3.3 低优先级

| 优化项 | 说明 | 预计工作量 |
|--------|------|------------|
| **密码修改页面** | 前端添加密码修改功能（云端已有API） | 2小时 |
| **移动端适配** | 优化响应式布局 | 4小时 |
| **暗黑模式** | 添加深色主题支持 | 4小时 |
| **E2E测试** | 添加端到端测试用例 | 8小时 |

---

## 四、持续更新机制 📋

### 4.1 代码质量保障

1. **测试覆盖**：每次提交前运行 `npm test`
2. **类型检查**：确保TypeScript编译无错误
3. **代码审查**：重要变更需要代码审查

### 4.2 开发流程

1. **功能开发**：在feature分支开发
2. **测试验证**：编写测试用例并验证
3. **代码合并**：合并到main分支
4. **自动部署**：GitHub Actions自动部署

### 4.3 监控与维护

1. **错误监控**：添加前端错误上报
2. **性能监控**：监控API响应时间
3. **日志分析**：定期分析错误日志

---

## 五、项目健康度评估（修复后）

| 维度 | 修复前 | 修复后 | 说明 |
|------|--------|--------|------|
| **功能完整性** | 8/10 | 8/10 | 核心功能完备 |
| **代码质量** | 6/10 | 7/10 | 减少重复代码，增强类型 |
| **测试覆盖** | 0/10 | 3/10 | 添加基础测试框架 |
| **安全性** | 6/10 | 8/10 | 修复关键安全漏洞 |
| **性能** | 7/10 | 8/10 | 优化关键性能问题 |
| **文档** | 8/10 | 8/10 | 文档完善 |
| **部署** | 9/10 | 9/10 | CI/CD自动化 |
| **可维护性** | 7/10 | 8/10 | 代码结构优化 |

**总体评分：7.5/10**（提升1分）

---

## 六、下一步行动

### 立即执行

1. 运行测试验证修复：`cd frontend && npm test`
2. 优化Board页面API请求
3. 添加更多测试用例

### 短期计划（1周内）

1. 完成TypeScript类型优化
2. 添加React Error Boundary
3. 实现日志清理机制

### 中期计划（1个月内）

1. 实现密码修改页面
2. 优化移动端适配
3. 添加更多测试用例

---

*计划生成完毕*
